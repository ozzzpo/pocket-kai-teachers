import { SearchTeacher } from '@/features';
import { Box } from '@chakra-ui/react';

export function SearchedTeachers() {
  return (
    <Box p="20px 0 10px 0">
      <SearchTeacher />
    </Box>
  );
}
